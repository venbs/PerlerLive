0.1.0
1. 鼠标在参数面板上操作时也会响应画布的拖拽，应当禁止
2. UI看起来很粗犷，不精致，感觉文字和UI组件都太巨大了，然后显得很笨。需要重新设计UI，让它看起来更精致，更高级。
3. 平滑抖动好像没有生效？是不是哪里算法有问题？
4. 去掉emoji先，prd里也去掉emoji的描述。
5. 复选框的样式不好看啊，需要重新设计。


0.3.0
1. 现在分辨率输入框UI好丑，和整体的UI样式不协调。需要重新设计。
2. 色板的每一条颜色项目的前面的色块太窄了，需要加宽一点，接近正方形。后面的十六进制代码输入框太长了，完全不需要那么长，十六进制的字体也太大了，可以小一些，并且颜色淡一点点。
3. 色板list的排序方式是根据颜色颗粒的使用数量需要从多到少排列。
4. 在移动端模式下，点击面板以外的地方，不会自动收起面板，需要支持这个功能。
5. 移动端模式下，“导入"功能面板收起的位置不对，因为tab是半透明的，还会露出来一些，你再计算一下位置，让它完全收起来。
6. 修改分辨率无效了？缺少提交功能。
7. 修改色板颜色也有bug，画布颜色没有更新，颜色直接跑到最底下去了，这块逻辑有很严重的问题。
8. hover在色块上时，色块有放大效果，我觉得这很好，但是放大后好像被父级容器裁切掉了？
9. 分辨率输入框右侧不需要有上下箭头按钮，输入数字后不要立刻提交，500ms没有输入任何内容后，再提交。